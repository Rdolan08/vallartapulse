Proceed with Phase 2b under the narrowest possible live-test scope.

Approved scope:
- composition wrappers only
- existing Airbnb/VRBO adapters remain untouched
- local DB only
- no Railway writes
- no GitHub push until results are reviewed
- coverage report included

First real live run constraints:
- source: Airbnb only
- neighborhood: Zona Romántica only
- seeds: 1
- max cards: 10
- max jobs: 1
- local DB only
- stop immediately on captcha / 403 / obvious block condition
- if blocked, mark terminationReason='blocked' and exit cleanly

Do not include VRBO in the first live run.
Do not expand to multiple sources until the Airbnb-only path is verified end-to-end.

Implementation scope:
1. Add composition wrappers:
   - airbnb-discovery-wrapper.ts
   - vrbo-discovery-wrapper.ts
   These should accept a single DiscoverySeed + YieldTracker and call the existing adapters underneath without rewriting them.

2. Add queue runner:
   - runDiscoveryLoop({ maxJobs, maxDurationMs, source?, region?, neighborhood?, maxResultsPerJob? })
   Requirements:
   - claimNext()
   - execute wrapper
   - process observations batch by batch
   - run YieldTracker logic
   - call upsertListing() + insertObservation()
   - markComplete() with termination reason
   - honor strict wall-clock budget
   - exit safely on blocked / parse failure / timeout

3. Wire CLI:
   - preserve dry-run behavior by default
   - only execute live discovery when --run is explicitly passed

4. First live command should be equivalent to:
   --run
   --source=airbnb
   --region=puerto-vallarta
   --neighborhood="Zona Romántica"
   --max-jobs=1
   --max-results-per-job=10

5. Deliverables after the first live run:
   - whether the seed executed successfully
   - number of cards seen
   - number of net-new listings inserted
   - dedup ratio
   - any parse failures
   - any block/captcha signals
   - termination reason
   - sample of inserted observations/listings from local DB
   - short recommendation on whether to proceed to a second micro-run

Optional second micro-run only if the first run succeeds cleanly:
- Airbnb only
- 1 additional seed
- 10 to 15 max cards
- still local DB only
- still no GitHub push until reviewed

Explicit non-goals for this session:
- no VRBO live run yet
- no broad discovery sweep
- no Railway writes
- no GitHub push before review