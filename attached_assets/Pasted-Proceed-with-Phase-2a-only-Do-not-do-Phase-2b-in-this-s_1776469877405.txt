Proceed with Phase 2a only.

Do not do Phase 2b in this session.
Do not wire the live Airbnb or VRBO adapters into the queue runner yet.
Do not run any live discovery against Airbnb or VRBO yet.
Keep this session strictly focused on durable discovery infrastructure and safe backfill.

Approved Phase 2a scope:

1. Seed generator
Implement a deterministic seed generator:
generateSeeds({ source, regions, neighborhoods? })

Requirements:
- support both airbnb and vrbo
- support region filtering
- support neighborhood filtering
- generate permutations across the approved Puerto Vallarta and secondary market neighborhood buckets
- include guest count, stay length, bedroom bucket, and check-in window permutations
- sort output by priority so Puerto Vallarta neighborhoods always come first
- make prioritization deterministic and reusable later by the runner

2. Queue manager
Implement queue helpers for discovery_jobs:
- insertSeeds()
- claimNext() using atomic locking / FOR UPDATE SKIP LOCKED
- markComplete()
- markFailed()
- saveCursor()
- resumePending()

Requirements:
- idempotent behavior wherever possible
- safe for repeated runs
- no dependence on live scraper execution yet
- compatible with later Phase 2b adapter wiring

3. Identity + dedup layer
Implement:
- computeIdentityKey()
- upsertListing()
- insertObservation()

Requirements:
- insert or update first_seen_at, last_seen_at, seen_count, lifecycle_status safely
- idempotent upsert behavior
- source-aware identity handling
- preserve raw location text and normalized neighborhood fields
- no duplicate listing creation for repeated seeds
- ready for later use by both discovery and enrichment

4. Yield tracker
Implement configurable per-seed exhaustion logic:
- two consecutive zero-yield batches => exhausted
- make threshold configurable
- store termination reason cleanly
- no live scraper dependency required yet

5. Backfill script for existing rental_listings
Implement an idempotent backfill for the current existing rows.

Backfill rules:
- first_seen_at = scraped_at where available
- last_seen_at = scraped_at where available
- seen_count = 1 if null
- lifecycle_status = 'active' if null
- compute identity key
- run neighborhood normalization against existing raw neighborhood/location data
- populate parent_region_bucket
- populate normalized_neighborhood_bucket
- populate neighborhood_mapping_confidence

Requirements:
- safe to rerun
- non-destructive
- no changes to existing business-critical columns beyond additive enrichment
- log how many rows were updated, skipped, or could not be confidently mapped

6. CLI scaffolding
Implement scripts/src/str-discovery.ts with support for:
- --source
- --region
- --neighborhood
- --dry-run
- --max-seeds
- --resume
- --seed-only

In Phase 2a:
- it may generate seeds
- insert seeds into the queue
- resume pending jobs
- run backfill
- print what would be scraped
- but it must not execute live scraping yet

7. Deliverables for this session
At the end of Phase 2a, provide:
- summary of code/files changed
- summary of schema usage
- sample seed output for Puerto Vallarta
- confirmation that Puerto Vallarta seeds are prioritized ahead of Riviera Nayarit
- backfill results for the current dataset
- example queue rows created
- example CLI commands to use for dry-run and seed-only testing

Explicit non-goals for this session:
- no live Airbnb scraping
- no live VRBO scraping
- no adapter rewiring
- no first discovery run
- no coverage run against real source traffic

After Phase 2a is complete and verified, we will do Phase 2b separately:
- adapter wrappers
- queue runner integration
- tight-budget live discovery
- first coverage report