Proceed with the next phase: merge Airbnb discovery + detail signals into stronger comp-ready inputs.

Do not change discovery.
Do not change the detail-enrichment worker.
Do not touch VRBO yet.
Do not add pagination, scrolling, retries, URL experiments, or new anti-bot work yet.

Goal:
Create a comp-ready normalized Airbnb listing signal layer that combines:
- discovery observations
- latest listing_details row (when parse_status='ok')
- derived nightly price
- neighborhood / region mapping

This phase should improve downstream pricing quality without requiring additional scraping behavior changes.

Scope:
1. Build a normalized Airbnb comp-signal view or materialization layer using:
   - rental_listings
   - latest relevant listing_search_observations row per listing
   - latest listing_details row per listing where parse_status='ok'

2. For each Airbnb listing, expose a merged normalized record with at least:
   Identity
   - listing_id
   - source_platform
   - external_listing_id
   - canonical_url

   Geography
   - normalized_neighborhood_bucket
   - parent_region_bucket
   - raw location hints if available
   - lat
   - lng

   Pricing
   - displayed_total_price
   - currency
   - stay_length_nights from the observation seed
   - derived_nightly_price
   - observed_at

   Quality / demand
   - displayed_rating or ratingOverall
   - displayed_review_count or reviewCount
   - imageCount

   Capacity / type
   - maxGuests
   - bedCount
   - propertyType
   - petsAllowed

   Content
   - title_displayed or title
   - description if available

3. Normalization rules:
   - prefer latest observation for price fields
   - prefer latest successful listing_details row for detail fields
   - prefer observation bucket/region mapping unless detail gives clearly better raw hints
   - preserve nulls where data is unknown
   - do not fabricate bedrooms/bathrooms/amenities if absent

4. Add derived comp-quality flags, such as:
   - has_price_signal
   - has_geo_signal
   - has_rating_signal
   - has_detail_signal
   - is_comp_usable_minimal
   - is_comp_usable_rich

5. Define comp usability conservatively:
   - minimal usable:
     - normalized_neighborhood_bucket
     - derived_nightly_price
     - currency
     - canonical_url
   - rich usable:
     - minimal usable plus at least 3 of:
       - rating/review_count
       - lat/lng
       - maxGuests
       - bedCount
       - propertyType
       - imageCount
       - title

6. Produce a report across the three PV neighborhoods already covered:
   - Zona Romántica
   - Amapas / Conchas Chinas
   - Centro / Alta Vista

Report:
- total Airbnb listings in each bucket
- count with price signal
- count with geo signal
- count with rating signal
- count with detail signal
- count minimal usable
- count rich usable
- average derived nightly by bucket
- median derived nightly by bucket if easy
- 5 sample merged comp rows

7. Keep this phase local-only:
- no Railway writes
- no GitHub push
- no schema changes unless truly necessary
- if possible, implement as a SQL view, query layer, or local materialization helper rather than mutating source tables

8. Constraints:
- discovery remains unchanged
- enrichment remains unchanged
- no new fetches required for this phase
- use only data already collected

Goal of this phase:
Turn the Airbnb data already captured into a comp-ready signal layer that can directly strengthen pricing logic and neighborhood analysis.