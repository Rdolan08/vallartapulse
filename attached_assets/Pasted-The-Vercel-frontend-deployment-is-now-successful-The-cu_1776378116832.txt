The Vercel frontend deployment is now successful. The current blocker is the Railway production backend/database.

Observed production behavior:
- frontend deploy is Ready/Latest on Vercel
- site shell loads
- data endpoints fail
- pricing tool shows API error HTTP 500
- Railway logs show DrizzleQueryError on multiple GET endpoints

Affected endpoints seen in Railway logs:
- /api/metrics/tourism
- /api/metrics/airport/estimates
- /api/metrics/rental-market

Important diagnosis:
This is not a Vercel output/build problem anymore. This is a production DB/schema mismatch or wrong production DB target. Drizzle is failing on SELECT queries, which strongly suggests one or more of:
1. Railway production DATABASE_URL points to the wrong database
2. latest migrations were not applied to Railway production
3. production is missing one or more tables/views/materialized views
4. production is missing renamed/new columns now expected by code
5. production seed/backfill jobs for metrics data were never run

Please do the following immediately:

1. Expand the FULL underlying SQL errors in Railway logs for each failing endpoint.
   I need the complete database error, not the truncated “Failed query: select ...” line.
   Specifically capture the actual Postgres message such as:
   - relation does not exist
   - column does not exist
   - materialized view does not exist
   - operator/type mismatch
   - invalid input syntax
   - permission denied

2. Verify Railway production environment variables:
   - DATABASE_URL
   - any SHADOW_DATABASE_URL / migration DB vars
   - any app env vars used to switch between local/dev/prod data sources

3. Confirm the app is connected to the intended production Postgres instance.
   Print/log the DB host/database name at startup in a safe way if needed.

4. Run production migrations against Railway now.
   Use the project’s real migration command, whichever applies in this repo, and confirm success.
   Then verify the schema contains every table/view queried by:
   - tourism metrics
   - airport estimates
   - rental market metrics
   - pricing tool dependencies

5. Compare Drizzle schema + current queries vs actual production schema.
   For each failing endpoint, identify the exact table/view and columns being selected.
   Then verify those exact objects exist in production.

6. If a table/view/materialized view is missing, create/rebuild it in production.
   If seed/backfill data is required for the metrics endpoints, run that too.

7. Add temporary defensive handling so the site does not hard-fail:
   - if a metrics query fails, return a structured fallback payload instead of HTTP 500 where reasonable
   - log the DB error with endpoint name and query target
   - keep the UI usable even if one metrics source is empty

8. After fixing Railway, verify these live pages load real data from production:
   - homepage/dashboard
   - /pricing-tool
   - /economic
   - /tourism
   - /rental-market

Working theory:
Production backend code and production database are out of sync. Fix the DB target and/or apply the missing schema/data changes in Railway production. Do not spend more time changing Vercel build settings.