(3) Restore both. Yes, check gitsafe-backup, but do it as a fast read-only check and do not let it become another analysis detour.

Proceed now with this exact approach:

1. Do a quick read-only check of gitsafe-backup to see whether it contains a cleaner pre-rollback state for main’s lost feature set.
   - If it clearly contains the fuller pre-rollback state for Set B, use it as a source reference.
   - If not, stop digging and continue immediately with main history + 3e41ffe.

2. Create ONE restore branch only. Do not touch main. Do not deploy.

3. Restore BOTH lost feature sets into that restore branch:
   - Set A from 3e41ffe / replit-agent:
     - market context UI
     - anomaly engine
     - market_events schema
     - separate unemployment chart
     - detailed YoY airport grid
   - Set B from main’s own pre-rollback commits/history:
     - hotel performance chart
     - ADR/RevPAR KPIs
     - cruise schedule scraper + UI
     - airport estimator
     - ESTIMATED/OFFICIAL badges
     - DATATUR economic rewrites
     - extended population history / related economic improvements

4. Preserve the currently working migration/deployment/runtime setup from current main:
   - MIGRATION.md
   - railway.json
   - vercel.json
   - artifacts/pv-datastore/src/lib/api-base.ts
   - artifacts/api-server/src/app.ts
   - artifacts/pv-datastore/src/main.tsx
   - artifacts/pv-datastore/src/pages/contact.tsx
   - artifacts/pv-datastore/src/pages/sources.tsx
   - artifacts/pv-datastore/src/pages/pricing-tool.tsx

5. pricing-tool.tsx stays on current main. Do not regress it.

6. For tourism.tsx, economic.tsx, dashboard.tsx, seed.ts, index.ts, and any related API/schema files:
   - build the combined version that restores both Set A and Set B
   - do not settle for Set A only
   - do not settle for Set B only
   - preserve current working production/runtime compatibility

7. Include any DB/schema/backend pieces required so the restored frontend features actually function:
   - anomaly-engine.ts
   - market_events.ts
   - schema index export
   - airport estimate hooks/types
   - seed logic
   - boot-time calls
   - any API handlers/scrapers/endpoints needed by the restored tourism/economic/airport/cruise features

8. After the restore branch is assembled:
   - run the normal local validation/build steps
   - make sure the branch is internally consistent
   - do not push to main
   - do not redeploy

9. Then report back only with:
   - git diff --stat main...restore-branch
   - exact restored/merged file list
   - any conflicts or compromises you had to make
   - anything still missing from either Set A or Set B

No more previews. No more branch-topology explanations. No more analysis unless something is actually blocked. Build the combined restore branch now.