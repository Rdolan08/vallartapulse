Proceed with the next phase: Airbnb listing-detail enrichment as a separate worker.

Do not change the working discovery flow.
Do not touch VRBO yet.
Do not add pagination, scrolling, retries, or URL experiments yet.
Keep discovery and enrichment separate.

Goal:
Use the existing discovered Airbnb listings as the input queue for a detail-enrichment worker that visits individual listing pages and stores richer normalized fields without changing discovery behavior.

Scope:
1. Add an Airbnb-only enrichment worker for already-known listings
2. Input set should come from rental_listings / listing_master rows where:
   - source = airbnb
   - canonical_url is present
   - listing_details is missing or stale
3. Use browser fetch mode for listing pages
4. Visit the listing page and extract, when present:
   - title
   - property type
   - bedrooms
   - bathrooms
   - guest capacity
   - amenities
   - latitude / longitude or map hints
   - host / manager label if present
   - review count
   - rating
   - image count
   - raw neighborhood/location hints
   - raw payload fragments useful for future repair
5. Persist results to listing_details only
6. Do not overwrite discovery observations
7. Do not make enrichment required for listing validity

Extraction rules:
- prefer structured embedded data over brittle selectors
- preserve raw fragments for debugging
- normalize fields conservatively
- null is acceptable where data is unclear

Testing plan:
Run a tiny local-only enrichment pass:
- source=airbnb
- max-listings=10
- only listings from:
  - Zona Romántica
  - Amapas / Conchas Chinas
  - Centro / Alta Vista

Report:
- listings attempted
- listings enriched successfully
- parse failures
- blocked count
- % with bedrooms
- % with bathrooms
- % with guest capacity
- % with amenities
- % with lat/lng
- 3 sample enriched rows

Constraints:
- local DB only
- no Railway writes
- no GitHub push
- no VRBO
- no discovery changes
- no pagination
- no retries unless a page outright fails once

After this phase, the next step will be:
- merge discovery + detail signals into stronger comp inputs
- then decide whether to broaden Airbnb coverage or re-approach VRBO