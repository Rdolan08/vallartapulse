Proceed with the next phase: deepen Airbnb card extraction only.

Do not change the working browser discovery flow.
Do not touch VRBO yet.
Do not add scrolling, retries, pagination, or listing-detail enrichment yet.
Keep discovery and enrichment separate.

Goal:
Improve the Airbnb search-card extractor so listing_search_observations captures richer card metadata when present in the rendered DOM or embedded JSON, while preserving the current reliable ID/link extraction.

Scope:
1. Keep the current browser-mode Airbnb discovery runner unchanged.
2. Upgrade only the Airbnb card parser/extractor to attempt to capture, per card:
   - title
   - displayed nightly price
   - displayed total price if present
   - rating
   - review count
   - thumbnail URL
   - raw location/neighborhood text if present
3. Continue to always capture:
   - external_listing_id
   - canonical_url
   - observed_at
   - normalized_neighborhood_bucket
   - parent_region_bucket
   - search_seed
   - raw_card_json

Extraction rules:
- Prefer stable structured data if available in the rendered page (embedded JSON/state blobs) before brittle CSS selectors
- Fall back to DOM selectors only when needed
- Do not fail the card if richer fields are missing
- The extractor should still count the card as valid if it only has:
  - listing ID
  - canonical URL
- Preserve nulls where data cannot be extracted confidently
- Store enough raw card payload to debug future parser drift

Success rule:
- A card with valid listing ID + canonical URL remains a successful observation even if title/price/rating are null
- Rich-field extraction is additive, not required for card validity

Testing plan:
Run three controlled Airbnb browser-mode tests against local DB only:

Run A
- source=airbnb
- region=puerto-vallarta
- neighborhood="Zona Romántica"
- max-jobs=2
- max-results-per-job=10
- fetch-mode=browser

Run B
- source=airbnb
- region=puerto-vallarta
- neighborhood="Amapas / Conchas Chinas"
- max-jobs=1
- max-results-per-job=10
- fetch-mode=browser

Run C
- source=airbnb
- region=puerto-vallarta
- neighborhood="Centro / Alta Vista"
- max-jobs=1
- max-results-per-job=10
- fetch-mode=browser

For each run, report:
- cards observed
- net-new listings
- duplicates
- parse failures
- blocked count
- termination reasons
- percentage of observations with non-null title
- percentage with non-null displayed price
- percentage with non-null rating
- percentage with non-null review count
- percentage with non-null thumbnail URL
- 3 sample observation rows showing the richer extracted fields

Constraints:
- local DB only
- no Railway writes
- no GitHub push
- no VRBO
- no pagination
- no scrolling
- no retries
- no listing-detail page visits yet

After this phase, if field coverage is good enough, the next phase will be:
- promote Airbnb search-card observations into a more useful pricing/discovery signal
- then separately build listing-detail enrichment as its own worker